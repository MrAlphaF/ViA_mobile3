<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark mode theme -->
    <style name="Theme.RepositoryG_LindaBrante_group3" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- App background -->
        <item name="android:colorBackground">@color/purple_background</item>

        <!-- Banner color -->
        <item name="colorPrimary">@color/purple_banner</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Button color -->
        <item name="colorSecondary">@color/purple_button</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Optional status bar -->
        <item name="android:statusBarColor" >@color/purple_banner</item>
    </style>
</resources>
