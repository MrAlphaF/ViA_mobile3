<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light mode theme -->
    <style name="Theme.RepositoryG_LindaBrante_group3" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- App background -->
        <item name="android:colorBackground">@color/blue_background</item>

        <!-- Banner color -->
        <item name="colorPrimary">@color/blue_banner</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Button color -->
        <item name="colorSecondary">@color/blue_button</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Optional status bar -->
        <item name="android:statusBarColor" >@color/blue_banner</item>
    </style>
</resources>
